query FetchCustomObjectDetails(
  $container: String,
  $key: String
  $id: String
) {
  customObject(
    container: $container, 
    key: $key
    id: $id
  ) {
    container
    id
    key
    value
  }
}
